import babel from 'rollup-plugin-babel';
import json from 'rollup-plugin-json';

export default {
  entry: 'src/main.js',
  sourceMap: true,
  plugins: [babel(), json()],
  dest: 'lib/main.js',
  format: 'cjs',
};
